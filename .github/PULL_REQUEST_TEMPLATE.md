## What changed?

<!-- One or two sentences. -->

## Checklist

- [ ] Corpus changes were made in `scripts/build-corpus.js`, not in generated files
- [ ] `npm run corpus:build` was run and both `corpus/corpus.json` and `corpus/PLAIN.txt` are committed
- [ ] `npm run check` passes locally
- [ ] New entries have a full-sentence `why` and correct `platforms` metadata
- [ ] Invisible characters in new entries are written as `\uXXXX` escapes

## Provenance (for new fixtures)

<!-- Where did this path hurt someone? Link the tool, issue, or story. -->
